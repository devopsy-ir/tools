version:  "3"

services:  
  prometheus:
    image: prom/prometheus
    volumes:
      - ./volumes/prometheus/data/:/prometheus
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - 9090:9090
    restart: always
